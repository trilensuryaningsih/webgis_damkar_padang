const express = require('express');
const router = express.Router();

// GET /api/rekomendasi (Placeholder)
router.get('/', (req, res) => {
  res.json({ message: 'Placeholder Route Rekomendasi' });
});

module.exports = router;
